package main;

import java.util.*;
public class Dice_Roll {
    /*public boolean isplayer(){
        Random rand = new Random();
        int roll = rand.nextInt(2,13);
        boolean status = false;
        if (roll == 7 || roll == 11){
            status = true;
        }
        else if (roll == 2 || roll == 3 || roll == 12){
            status = false;
        }
        else{
            int roll2 = 0;
            ArrayList<Integer> results = new ArrayList<>();
            results.add(roll);
            results.add(7);
            results.add(11);
            while (!results.contains(roll2)){
                roll2 = rand.nextInt(2,13);
                if (roll == roll2){
                    status = true;
                }
                if (roll2 == 7 || roll2 == 11){
                    status = false;
                }
            }
        }
        return status;
    }
    /*
        def is_opponent:
        roll = random.randrange(2,13)
        status = False
        if roll == 7 or roll == 11:
        status = True
        elif roll == 2 or roll == 3 or roll == 12:
        status = False
        roll2 = random.randrange(2,13)
        while status != False and status != True:
        if roll == roll2:
        status = True
        if roll2 == 7 or roll2 == 11:
        status = False
        return status

        def diceroll():
        p1 = is_player()
        p2 = is_opponent()
        str = ''
        if p1 == False and p2 == True:
        str = 'You Lose!'
        if p1 == True and p2 == False:
        str = 'You win!'
        if p1 == False and p2 == False or p1 == True and p2 == True:
        str = 'Try Again!'
     */
    public static void main(String[] args) {

    }
}
